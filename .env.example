# Copy to .env.local (gitignored) for local OpenRouter checks only.
# Never commit .env.local. Add the key in Settings for the app UI.
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Public OSS / Vercel deploy profile (math + CS + probability only; no SAT or college).
# VITE_APP_PROFILE=public
