# Copy to .env.local (gitignored) for local OpenRouter checks only.
# Never commit .env.local. Add the key in Settings for the app UI.
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# SAT curriculum + study surfaces (default: included locally).
# Public Vercel deploy sets this to false via vercel.json.
# VITE_INCLUDE_SAT=true
