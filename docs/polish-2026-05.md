# Post-roadmap polish — 2026-05-25

Baseline: **v2.3.56** after batches 91–111. Desktop daily driver at http://127.0.0.1:8080.

## Shipped in v2.3.56

- Today **Recent study** listens to activity + data-sync events (no reload needed).
- ⌘K splits **Recent actions** vs **Recent searches**.
- Mobile top bar shows **Learn v2** branding (sidebar hidden on small screens).
- Office-hours empty review uses shared **EmptyState**.
- `npm start` alias for `npm run dev`.
- Pages deploy job `continue-on-error` when GitHub plan lacks Pages.

## Gate

```bash
npm run test
npm run build
npm run curriculum:lint
npm run test:e2e   # optional; needs preview or dev on 8080 for webServer
```

## Manual smoke (5 min)

- [ ] `/` — complete a lesson → Recent study updates without refresh
- [ ] Office hours — save a prompt → TA review shows notes diff panel
- [ ] ⌘K — run two different jumps → both appear under Recent actions
- [ ] Stats — week-in-review line + calendar filter
